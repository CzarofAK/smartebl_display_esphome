# =============================================================================
#  smart_ebl_pages/page_sensors_overview.yaml -- page: Sensors, sub-page 1 (Overview)
#  Smart EBL Display (ESP32-P4) - page package
# =============================================================================
# docs/pages.md §5/§7: honest placeholder - the real door/window/presence
# entity list isn't enumerated yet. Exists (with its own nav entry + "1/2"
# indicator) so sub-page 2 (Levelling, page_sensors_levelling.yaml, real)
# is reachable without waiting on this page's content. Replace this
# page's widgets once that entity list exists - no navigation wiring
# needed here, that already lives in nav_show_sensors (main device file).
# =============================================================================

lvgl:
  pages:
    # =========================================================================
    # Sensors, sub-page 1 (page_sensors_overview) - pages.md §5/§7's own
    # open item: "which specific door/window/presence entities exist and
    # how many fit the grid template isn't enumerated yet - needs the
    # actual HA entity list before that page can be more than a
    # placeholder." Built as an honest placeholder rather than skipped
    # outright, so the SENSORS nav entry + its page-indicator exist and
    # sub-page 2 (Levelling, real) is reachable - filling this in is
    # "replace this page's content", not "wire up new navigation".
    - id: page_sensors_overview
      bg_color: 0x000000
      bg_opa: COVER
      widgets:
        - label:
            align: CENTER
            y: -20
            text: "SENSOREN"
            text_font: font_ui_24
            text_color: 0x707070
        - label:
            align: CENTER
            y: 20
            width: 700
            text_align: CENTER
            text: "Inhalt noch offen - Entity-Liste fehlt noch\n(docs/pages.md §7)"
            text_font: font_ui_16
            text_color: 0x555555

