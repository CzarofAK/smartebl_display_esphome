# Smart EBL Display (ESP32-P4) - ESPHome Secrets Template
# Copy this file to secrets.yaml and fill in your values
# NEVER commit secrets.yaml to version control!

# Home Assistant API encryption key (generate with: openssl rand -base64 32)
api_encryption_key: "generate-a-random-32-byte-base64-key"

# OTA update password
ota_password: "your-ota-password"

# Not used today (Ethernet is the primary network path - see README.md
# "Why Ethernet, not Wi-Fi 6"). Kept here so a future wifi: block has
# somewhere to read from without re-touching this file.
# wifi_ssid: "your-wifi-ssid"
# wifi_password: "your-wifi-password"
